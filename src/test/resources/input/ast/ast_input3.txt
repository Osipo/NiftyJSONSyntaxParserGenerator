{
    a = 15;
    b = a * 2 ^ 2 ^ 3;
    {
        list = [1, 2, 'c', lambda (x) => { x = 10; lambda(y) => x * y;;}, "hello world", [5, 6, [7, "end"], 10], 100];

        list[1] = lambda (a, b) => { a + b; };  # first ; close function body, second ; close whole operator (assign stmt)

        f = list[1];
        g = lambda (x) => lambda(y) => y * x;;;
        list[0] = f(a * 2, f(10, 20)); # 60.
        q = f(a, b);
        q2 = f(list[0], f);
    }
}

# f = lambda(x) => lambda(y) => x * y;;;
# g = f(x);
# r = g(y);