{
    a = 2 * 2 ^ 2 ^ 3; // 512
    b = a - 2; // 510.
    co = (a == b); // 0.
    co2 = (a == 512); // 1.

    list = [1, 2 + 2 * 2 ^ 2, a, [4, 5, [6]], 'c'];

    fibo = lambda (n) => {
        if(n == 1 || n == 2)
            1;
        else
            if(n <= 0)
                -1;
            else
                fibo(n - 1) + fibo(n - 2);
    };
    f1 = fibo(-999);
    f2 = fibo(1);
    f3 = fibo(2);
    f4 = fibo(3); // call fibo(2) + fibo(1) => 1 + 1 => 2.
    ch = 'c';
    f5 = fibo(4); // call fibo(3) + fibo(2) => 2 + 1 => 3.
    f6 = fibo(5); // => 5.
}