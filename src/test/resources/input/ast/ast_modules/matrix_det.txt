module MatrixDet
from MyModule.Math import *
{
    /*
    M = [[4, 2, 5], [6, 4, 9], [7, 6, 13]];
    n = len(M);
    E = E_ML(n); //[gen [gen eq(x, y) for y in [1..n] end] for x in [1..n] end]

    M = [gen M[i] + E[i] for i in [..n - 1] end];
    MC = cols(M);
    MC = [gen MC[i][0] + 1000 for i in [0..len(MC) - 1] end];

    L = [1, 2, 3, 4, 5];
    L = [5 - L];
    L = L[0];

    M_R = inverse_m(M);
    */

    L = [ [1], 2, 3];
    a = L[0][0];

    b = "abcd"[1];
    c = [1, 2, 3][2, 0][0]; // [1, 2, 3] => [3, 1] => [1].

    //det = 1 / M_R[0][0];
}