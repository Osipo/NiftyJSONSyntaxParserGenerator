module MatrixDet
from MyModule.Math import *
{
    M = [[4, 2, 5], [6, 4, 9], [7, 6, 13]];
    E = E_ML(len(M));
    ME = M + E;

    TME = cols(ME);
    TT = TME[0] + 100;
    TME = TME;
    TME[0] += 100;
    TME = TME;

    ff = lambda(n) => {
        [gen [gen eq(x, y) for y in [1..n] end] for x in [1..n] end];
    };

    E2 = ff(4);
}